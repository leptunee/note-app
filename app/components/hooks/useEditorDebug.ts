// 编辑器调试工具 - 生产环境版本（已禁用调试输出）

export function useEditorDebug(editor: any) {
  // 生产环境下禁用调试功能
  // 如需启用调试，请取消注释下面的代码并添加必要的 console.log 语句
  
  /* 
  useEffect(() => {
    if (!editor) return;
    // 在这里添加调试代码
  }, [editor]);
  */
}
